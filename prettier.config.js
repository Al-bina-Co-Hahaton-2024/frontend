module.exports = {
    // Использование точки с запятой в конце строк
    semi: true,
    // Длина строки
    printWidth: 80,
    // Одинарные кавычки для строк
    singleQuote: true,
    // Табы вместо пробелов
    useTabs: false,
    // Отступ 2 пробела
    tabWidth: 2,
    // Висячая запятая в многозначных литералах
    trailingComma: 'es5',
    // Пробелы между скобками в объектах
    bracketSpacing: true,
    // Управление переносом скобок в JSX
    jsxBracketSameLine: false,
    // Управление переносом стрелочных функций
    arrowParens: 'always',
    // Форматирование всего содержимого файла
    endOfLine: 'lf',
};
